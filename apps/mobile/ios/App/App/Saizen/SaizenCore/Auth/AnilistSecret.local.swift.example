import Foundation

/// LOCAL ONLY — copy to `AnilistSecret.local.swift` and set your AniList Client Secret.
/// That file is gitignored. Never use NEXT_PUBLIC_* for secrets.
enum AnilistSecretLocal {
  /// Paste the Client Secret from anilist.co/settings/developer (app ID must match NEXT_PUBLIC_ANILIST_CLIENT_ID).
  static let value: String? = nil
}
